Graph is a graph that contains edges and vertexes. In this graph edges and vertexes can be added and removed while they are represented in an adjacency list.
This project implements DFS, BFS travesals, Minimum Spanning Tree and Djikstra algorithms.
